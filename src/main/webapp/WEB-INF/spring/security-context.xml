<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
	
	<bean id ="customAccessDenied" class="org.shiftworks.security.CustomAccessDeniedHandler"/>
	
	<bean id="bcryptPasswordEncoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
	
	<bean id="customUserDetailsService"
		class="org.shiftworks.security.CustomUserDetailsService"/>
	
	<security:http>
	
		<security:intercept-url pattern="/login/all" access="permitAll"/>
		<security:intercept-url pattern="/login/customLogin" access="isAnonymous()"/>
		<security:intercept-url pattern="/login/member" access="hasRole('user')"/>
		<security:intercept-url pattern="/login/admin" access="hasRole('admin')"/>

		<!-- <security:form-login/> -->
		<security:form-login 
			 username-parameter="emp_id"
			 password-parameter="password"
			 login-page="/login/customLogin"
			 default-target-url="/로그인 성공시 보내줄 페이지"/>
		
		<security:logout logout-url="" 
			invalidate-session="true"
			delete-cookies="JSESSION_ID"/>
	<!-- <security:access-denied-handler error-page="/accessError"/> -->
	<security:access-denied-handler ref="customAccessDenied"/>
	
	</security:http>
	
	<!-- 커스텀 UserDetailsService 활용-->
 		<security:authentication-manager>
		<security:authentication-provider user-service-ref="customUserDetailsService">
			<security:password-encoder ref="bcryptPasswordEncoder" />
		</security:authentication-provider>
	</security:authentication-manager>

</beans>
